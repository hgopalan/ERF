FILE = inputs_base

# Additive with open-fraction placement, flat ground, and the energy-consistent
# tendency form: the same placement as add_noib_open with the energy ratio
# exactly one.
erf.fire.source_mode = "add"
erf.fire.heat_open_fraction = true
erf.fire.heat_tendency_density = false
