FILE = inputs_base

# Additive with open-fraction placement, immersed-forcing buildings, and the
# energy-consistent tendency form: the configuration a WUI run should use.
# Immersed-forcing buildings from the same heightmap the structure mask reads
erf.buildings_type = ImmersedForcing
erf.buildings_file_name = buildings_5m_64x32.txt
erf.immersed_forcing_substep = true
eb2.small_volfrac = 0.005
erf.if_Cd_momentum = 500.0
erf.if_Cd_scalar   = 50.0
erf.fire.source_mode = "add"
erf.fire.heat_open_fraction = true
erf.fire.heat_tendency_density = false
