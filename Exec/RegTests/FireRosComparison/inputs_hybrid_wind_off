FILE = inputs_base

# Identity: a wind band far above the midflame wind leaves the weight at 0
# everywhere, so the hybrid reproduces rothermel_isotropic exactly.
erf.fire.ros_model = "hybrid"
erf.fire.directional_ros = false
erf.fire.hybrid.primary   = "rothermel"
erf.fire.hybrid.secondary = "balbi"
erf.fire.hybrid.selector  = "wind"
erf.fire.hybrid.wind_lo   = 50.0
erf.fire.hybrid.wind_hi   = 60.0
erf.fire.balbi.formulation = "2020"
