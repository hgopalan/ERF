FILE = inputs_base

# Restarts from chk00200 (written by inputs_coupled_chk) and runs to 200 s.
# Must reproduce inputs_coupled_straight.
erf.fire.ros_model = "rothermel"
erf.fire.coupling_type = "lagged"
erf.fire.directional_ros = true
erf.fire.propagation_method = "levelset"
erf.restart = chk00200
stop_time = 200.0
max_step = 400
erf.check_int = -1
