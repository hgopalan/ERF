FILE = inputs_base

# Restarts from chk00197 (written by inputs_levelset_chk) and runs to 200 s.
# Must reproduce inputs_levelset_straight.
erf.fire.ros_model = "balbi"
erf.fire.balbi.formulation = "2020"
erf.fire.directional_ros = true
erf.fire.propagation_method = "levelset"
erf.fire.coupling_type = "passive"
erf.restart = chk00197
stop_time = 200.0
max_step = 400
erf.check_int = -1
