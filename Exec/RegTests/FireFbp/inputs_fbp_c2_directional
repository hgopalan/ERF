FILE = inputs_base

# C-2 with the rate evaluated along the front normal: the head rate is the isotropic one.
erf.fire.ros_model = "fbp"
erf.fire.fbp.fuel_type = "C2"
erf.fire.fbp.ffmc = 90.0
erf.fire.fbp.bui = 60.0
erf.fire.directional_ros = true
