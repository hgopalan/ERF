FILE = inputs_base

# The plane z = 0.6 x (31.0 degrees), directional level set (the default):
# the slope is taken along the front normal, so backing and flanks spread at R0.
erf.fire.terrain_file_name = "plane_s60.txt"
