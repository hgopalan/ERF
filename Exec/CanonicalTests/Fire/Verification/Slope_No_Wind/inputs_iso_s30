FILE = inputs_base

# The plane z = 0.3 x (16.7 degrees), isotropic level set: the whole front
# spreads at Rothermel's head rate R0 (1 + phi_s), projected onto the ground.
erf.fire.terrain_file_name = "plane_s30.txt"
erf.fire.directional_ros = false
